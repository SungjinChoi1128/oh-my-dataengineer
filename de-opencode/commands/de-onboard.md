---
description: Initialize or refresh data-engineering repo context
agent: data-engineer
---

Run `de repo doctor`. If context is missing, run `de repo init`, then read `de repo contract`, `de repo map`, `de repo brief`, `de repo todo`, and the highest-priority `de repo interview` questions. If context exists, use the current context and mention `de repo reset` only when the repo appears to be a shared integration repo, branch switch, or stale context case.
